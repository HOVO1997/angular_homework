import { HotkeysBlockerDirective } from './hotkeys-blocker.directive';

describe('HotkeysBlockerDirective', () => {
  it('should create an instance', () => {
    const directive = new HotkeysBlockerDirective();
    expect(directive).toBeTruthy();
  });
});
